import 'package:flutter/material.dart';

class Pallete {
  // Backgrounds
  static const Color backgroundColor = Color(0xFFF9F9F9); // Light app background
  static const Color cardColor = Colors.white; // Form card

  // Primary colors
  static const Color greenColor = Color(0xFF95BF47); // Register button & links
  static const Color errorColor = Colors.redAccent;

  // Text
  static const Color whiteColor = Colors.white;
  static const Color blackColor = Colors.black;
  static const Color subtitleText = Color(0xFF7C8BA0); // Light gray subtitle
  static const Color linkText = Color(0xFF95BF47); // "Login" text

  // Borders & Input Fields
  static const Color inputFillColor = Color(0xFFF5F5F5); // Input background
  static const Color borderColor = Color(0xFFE0E0E0);    // Input border

  // Icons
  static const Color iconColor = Color(0xFFBDBDBD); // Password visibility icon

  // Bottom bar (if used)
  static const Color inactiveBottomBarItemColor = Color(0xffababab);

  // Gradients (used in top dark header)
  static const Color gradient1 = Color(0xFF0F1B2B); // Dark top background
  static const Color gradient2 = Color(0xFF0F1B2B); // Same for simplicity
  static const Color gradient3 = Color(0xFF0F1B2B); // Same for simplicity
  static const Color limeColor = Color.fromARGB(255, 202, 228, 72);


  // Others
  static const Color greyColor = Colors.grey;
  static const Color transparentColor = Colors.transparent;
  static const Color inactiveSeekColor = Colors.black26;
}
